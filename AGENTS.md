# How to run commands

- use henv tool to run things within pythons venv
- prepend each command with: henv -n export_codex_session_dev -x <command>
- you can install python tools within this venv w/o asking with henv -n export_codex_session_dev -x python -m pip install <something>
